/*#include <iostream>

using namespace std;

int main() {

	//declare variables
	double width, height, length;

	//information gathering
	cout << "Provide the width: ";
	cin >> width;
	cout << "Provide the height: ";
	cin >> height;
	cout << "Provide the length: ";
	cin >> length;

	//determine if package is rejected or accepted
	if (width > 10 || height > 10 || length > 10)
		cout << "Package is rejected!" << endl;
	else
		cout << "Package is accepted!" << endl;
	return 0;
}
*/