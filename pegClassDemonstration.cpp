/*
#include <pegclass>
#include <utility>

int main()
{
	PegClass P(7), Q(7);
	P.AddRing(3); Q.AddRing(2);
	P.AddRing(4); Q.AddRing(3);
	P.AddRing(5); Q.AddRing(4);
	P.ShowAll(); Pause();
	P.RemoveRing();
	P.RemoveRing(); Q.RemoveRing();
	P.ShowAll();
	return(0);
}
*/