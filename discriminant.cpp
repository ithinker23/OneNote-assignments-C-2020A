/*
#include <iostream>

using namespace std;

int main() {

	//declare variables
	double a, b, c, amountOfRoots, discriminant;

	//information gathering
	cout << "What is the value of a: ";
	cin >> a;
	cout << "What is the value of b: ";
	cin >> b;
	cout << "What is the value of c: ";
	cin >> c;
	cout << endl;

	//calculations
	discriminant = (b * b) - 4 * (a) * (c);
	
	//determine amount of roots
	if (discriminant > 0)
		amountOfRoots = 2;

	else if(discriminant < 0)
		amountOfRoots = 0;

	else
		amountOfRoots = 1;


	//display output
	cout <<"a:" << a << " b:" << b << " c:" << c << endl;
	cout << "the amount of roots is: " << amountOfRoots << endl;

	return 0;
}*/