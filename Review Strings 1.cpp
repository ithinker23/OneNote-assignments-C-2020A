/*
#include <iostream>
#include <string>

using namespace std;

void WriteFirstLast(string name) {

	int commaPos = name.find(",");
	string firstName = name.substr(commaPos + 2, name.length());
	string lastName = name.substr(0, commaPos);

	cout << firstName << " " << lastName << endl;
}


int main(){

	WriteFirstLast("HI THIS IS A LAST NAME, THIS IS MY FIRST NAME");

	return 0;
}
*/
